<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Успешное создание </title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.css">
    <style>
        .celebration-icon {
            font-size: 5rem;
            color: #28a745;
            margin-bottom: 20px;
        }
        .message {
            font-size: 1.25rem;
            color: #333;
        }
        .btn-custom {
            padding: 15px 30px;
            font-size: 1.1rem;
            border-radius: 25px;
        }
    </style>
</head>
<body class="bg-light">

<div class="container d-flex justify-content-center align-items-center" style="height: 100vh;">
    <div class="text-center">
        <i class="bi bi-check-circle celebration-icon"></i>
        <h1>Поздравляем!</h1>
        <p class="message">Успешно создано!</p>

        <div class="mt-4">
            <a href="/profile" class="btn btn-success btn-custom">Перейти в профиль</a>
        </div>
        <div class="mt-3">
            <a href="/resumes/new" class="btn btn-outline-primary btn-custom">Создать новое резюме</a>
        </div>
    </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
